# ☕ Coffee Shop Management System

ยินดีต้อนรับสู่โปรเจกต์ระบบจัดการร้านกาแฟของเรา! 
ไฟล์นี้จะสรุปขั้นตอนตั้งแต่การติดตั้งเครื่องมือ ไปจนถึงวิธีแก้ Error เบื้องต้น เพื่อให้ทุกคนในทีมสามารถรันโปรเจกต์นี้ในเครื่องตัวเองได้แบบไม่ติดขัดครับ 🚀

---
 #### ห้ามลืม สร้าง branch ตัวเองเด็ดขาด ####

## 📂 โครงสร้างโปรเจกต์ (Project Structure)
โปรเจกต์ของเราแบ่งโฟลเดอร์ไว้ชัดเจน ดังนี้:
- `/coffee_shop/backend` 👉 **(หลังบ้าน)** ใช้ Node.js + Express (ไฟล์หลักคือ `server.js`)
- `/coffee_shop/frontend` 👉 **(หน้าบ้าน)** ใช้ React (หน้าตาเว็บทั้งหมดจะอยู่ที่นี่)
- `/coffee_shop/database` 👉 เก็บไฟล์ที่เกี่ยวกับฐานข้อมูล (เช่น โค้ด SQL สร้างตาราง)
- **ไฟล์นอกสุด (Root)** 👉 เป็นตัวจัดการระบบ เช่น `.gitignore` และ `package.json` สำหรับสั่งรันรวดเดียว

---

## 🛠️ เครื่องมือที่ต้องมี (Prerequisites)
ก่อนเริ่มทำงาน ทุกคนต้องติดตั้งโปรแกรมพื้นฐานเหล่านี้ในเครื่องก่อน:
1. **Node.js**: สำหรับรันเซิร์ฟเวอร์หลังบ้านและหน้าบ้าน (แนะนำเวอร์ชัน LTS)
2. **Git**: สำหรับดึงโค้ดและส่งโค้ดขึ้น GitHub
3. **VS Code (Visual Studio Code)**: โปรแกรมเขียนโค้ดหลักของทีมเรา
4. **ส่วนขยาย (Extensions) แนะนำใน VS Code**: 
   - `Prettier` (ช่วยจัดฟอร์แมตโค้ดให้สวย)
   - `ES7+ React/Redux/React-Native snippets` (สำหรับคนทำ Frontend)

---

## 🚀 ขั้นตอนการติดตั้งและรันโปรเจกต์ (Getting Started)

**1. ดึงโค้ดลงเครื่อง (Clone Project)**
เปิด Terminal ขึ้นมาแล้วพิมพ์:
```bash
git clone <ใส่-URL-ของ-GitHub-โปรเจกต์นี้>
cd Coffee-Shop-Management-System/coffee_shop
2. ติดตั้งเครื่องมือ (Install Dependencies)
ห้ามข้ามขั้นตอนนี้เด็ดขาด! เพราะเราต้องโหลด Library ของทั้งหน้าบ้านและหลังบ้านก่อน:

Bash
# โหลดของโฟลเดอร์หลัก
npm install

# โหลดของหลังบ้าน
cd backend
npm install express

# โหลดของหน้าบ้าน
cd ../frontend
npm install
3. วิธีรันโปรเจกต์ (Run Project)
กลับมาที่โฟลเดอร์หลัก (coffee_shop) แล้วใช้คำสั่งนี้คำสั่งเดียว ระบบจะเปิดทั้ง Backend และ Frontend ให้พร้อมกันเลย!

Bash
npm start
Backend จะรันอยู่ที่: http://localhost:3000

Frontend จะรันอยู่ที่: http://localhost:3000 (หรือพอร์ตที่ React กำหนด เช่น 3001)

🚨 รวมฮิตวิธีแก้ Error (Troubleshooting)
ถ้าทำตามแล้วพัง ลองดูวิธีแก้ตรงนี้ก่อนนะ!

Error 1: สั่งรันเว็บแล้วมันวนลูปค้าง (Loop นรก) หรือหาไฟล์ไม่เจอ

สาเหตุ: เผลอไปกด npm start ผิดโฟลเดอร์ หรือในโฟลเดอร์นั้นยังไม่ได้สร้างไฟล์โปรเจกต์

วิธีแก้: เช็คให้ชัวร์ว่าเราอยู่ในโฟลเดอร์ที่ถูกต้องเสมอ (ดูที่บรรทัดสุดท้ายของ Terminal ว่าเราอยู่ที่ .../coffee_shop หรือยัง)

Error 2: Error: Cannot find module 'express'

สาเหตุ: ลืมกด npm install เพื่อโหลดเครื่องมือลงมา

วิธีแก้: มุดเข้าไปที่โฟลเดอร์นั้นๆ (เช่น cd backend) แล้วพิมพ์ npm install เพื่อให้มันสร้างโฟลเดอร์ node_modules ขึ้นมาใหม่

Error 3: Git ล็อคตัวเอง ฟ้องตัวแดงว่า fatal: Unable to create .../.git/index.lock': File exists.

สาเหตุ: อาจจะปิด Terminal กะทันหันตอน Git กำลังเซฟ หรือมีคำสั่ง Git ค้างอยู่

วิธีแก้: พิมพ์คำสั่งนี้ใน Terminal เพื่อปลดล็อค:

Bash
rm .git/index.lock
จากนั้นค่อยรันคำสั่ง git add . และ commit ใหม่อีกรอบ


ลองก๊อปปี้ไปสร้างไฟล์ดูนะครับ ถ้าเสร็จแล้ว **อยากให้ผมช่วยตรวจความเรียบร้อยของไฟล์ `package.json` ตัวนอกสุดให้ไหมครับ** ว่าตั้งค่ารันคู่ไว้ถูกต้องหรือยัง?